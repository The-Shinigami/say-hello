pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages{

      stage('deploy') {
            steps {
                dir('target') {
                 sh 'echo $PWD' 
                 sh 'ls'
               }
                
            }
        }
    }        
    
}
