pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages{

      stage('deploy') {
            steps {
                dir('target') {
                 sh 'java -jar say-hello-0.0.1.jar' 
               }
                
            }
        }
    }        
    
}
